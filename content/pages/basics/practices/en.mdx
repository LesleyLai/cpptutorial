---
title: "Practice Problems"
---

1. Write a function `to_fahrenheit` that reads the temperature in Celcius and returns the corresponding temperature in Fahrenheit.
It should take a `double` and returns a `double`. The conversion function is the following:

$
f = c * \frac{9}{5} + 32
$

2. Leibniz formula for π
